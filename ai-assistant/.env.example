GEMINI_API_KEY=your_gemini_api_key_here

# Must match the .NET API's Jwt:SigningKey user-secret exactly (see
# TelematicsPipeline.Api: `dotnet user-secrets list`).
JWT_SIGNING_KEY=your_jwt_signing_key_here
JWT_ISSUER=telematics-api
JWT_AUDIENCE=telematics-dashboard
